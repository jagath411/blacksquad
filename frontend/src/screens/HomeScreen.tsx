import { StyleSheet,Text,type TextStyle } from 'react-native';import type { NativeStackScreenProps } from '@react-navigation/native-stack';import { Screen } from '../components/Screen';import { Card,CardContent,CardHeader } from '../components/ui';import { colors } from '../constants/theme';import type { RootStackParamList } from '../types';
type Props=NativeStackScreenProps<RootStackParamList,'Home'>;
export function HomeScreen({route}:Props){const driver=route.params.role==='DRIVER';return <Screen><Text style={s.kicker}>{driver?'DRIVER MODE':'CUSTOMER MODE'}</Text><Text style={s.title}>{driver?'Ready for the road?':'Where are you going?'}</Text><Card tone="dark"><CardHeader><Text style={s.cardTitle}>{driver?'Availability controls':'Book your first trip'}</Text></CardHeader><CardContent><Text style={s.body}>{driver?'Online status, booking requests, and live location are added after secure authentication.':'Pickup, destination, and live driver tracking are added after secure authentication.'}</Text><Text style={s.badge}>FOUNDATION READY</Text></CardContent></Card></Screen>}
const s=StyleSheet.create<{kicker:TextStyle;title:TextStyle;cardTitle:TextStyle;body:TextStyle;badge:TextStyle}>({kicker:{color:colors.green,fontSize:11,letterSpacing:2,fontWeight:'700',marginTop:45},title:{fontSize:40,lineHeight:46,color:colors.text,fontWeight:'800',marginTop:12,marginBottom:35},cardTitle:{fontSize:20,color:colors.text,fontWeight:'700'},body:{fontSize:15,lineHeight:23,color:colors.muted},badge:{fontSize:10,letterSpacing:1.5,color:colors.green,fontWeight:'800'}});
