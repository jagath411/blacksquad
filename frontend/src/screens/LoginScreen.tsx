import { useEffect,useState } from 'react';import { StyleSheet,Text,TextInput,View,type TextStyle,type ViewStyle } from 'react-native';import type { NativeStackScreenProps } from '@react-navigation/native-stack';
import { Screen } from '../components/Screen';import { AppButton } from '../components/AppButton';import { colors } from '../constants/theme';import { getHealth } from '../services/api/health';import type { RootStackParamList } from '../types';
type Props=NativeStackScreenProps<RootStackParamList,'Login'>;
export function LoginScreen({route,navigation}:Props){const [api,setApi]=useState('Checking API…');useEffect(()=>{getHealth().then(()=>setApi('Backend connected')).catch(()=>setApi('Backend unavailable'))},[]);return <Screen><View style={s.header}><Text style={s.kicker}>{route.params.role} PORTAL</Text><Text style={s.title}>Welcome back.</Text><Text style={s.status}>{api}</Text></View><View style={s.form}><TextInput style={s.input} placeholder="Email address" placeholderTextColor={colors.muted} autoCapitalize="none"/><TextInput style={s.input} placeholder="Password" placeholderTextColor={colors.muted} secureTextEntry/><AppButton label="Continue" onPress={()=>navigation.navigate('Home',{role:route.params.role})}/><Text style={s.note}>Authentication UI is ready; submission activates after the backend auth phase.</Text></View></Screen>}
const s=StyleSheet.create<{header:ViewStyle;kicker:TextStyle;title:TextStyle;status:TextStyle;form:ViewStyle;input:TextStyle;note:TextStyle}>({header:{marginTop:60,marginBottom:48},kicker:{color:colors.green,fontSize:11,letterSpacing:2,fontWeight:'700'},title:{fontSize:42,color:colors.text,fontWeight:'800',marginTop:12},status:{color:colors.muted,marginTop:12},form:{gap:13},input:{height:56,borderRadius:16,borderWidth:1,borderColor:colors.border,backgroundColor:colors.surface,paddingHorizontal:17,color:colors.text,fontSize:16},note:{color:colors.muted,fontSize:12,lineHeight:18,textAlign:'center',marginTop:5}});
