import { StyleSheet,Text,View,type TextStyle,type ViewStyle } from 'react-native';
import type { NativeStackScreenProps } from '@react-navigation/native-stack';
import { Screen } from '../components/Screen';import { AppButton } from '../components/AppButton';import { colors } from '../constants/theme';import type { RootStackParamList,UserRole } from '../types';
type Props=NativeStackScreenProps<RootStackParamList,'Role'>;
export function RoleScreen({navigation}:Props){const select=(role:UserRole)=>navigation.navigate('Login',{role});return <Screen><View style={s.header}><Text style={s.kicker}>TRANSPORT OPERATIONS</Text><Text style={s.title}>What do you manage?</Text><Text style={s.body}>Choose the workspace for your role. Owners manage the fleet; drivers share their live trip status.</Text></View><View style={s.cards}><AppButton label="I am a transport owner" onPress={()=>select('OWNER')}/><AppButton label="I am a driver" variant="secondary" onPress={()=>select('DRIVER')}/></View></Screen>}
const s=StyleSheet.create<{header:ViewStyle;kicker:TextStyle;title:TextStyle;body:TextStyle;cards:ViewStyle}>({header:{flex:1,justifyContent:'center'},kicker:{color:colors.green,fontSize:11,letterSpacing:2,fontWeight:'700'},title:{color:colors.text,fontSize:38,lineHeight:44,fontWeight:'800',marginTop:14},body:{color:colors.muted,fontSize:16,lineHeight:24,marginTop:18},cards:{gap:12,paddingBottom:16}});
